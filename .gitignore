# Claude Code — user-specific, not project code
.claude/

# Internal planning and session docs — local only
HANDOFF.md
CLAUDE.md

# Build output
dist/
metafile.json
runtime-npm-modules.txt

# Generated spec file (built from packaging/*.spec.in)
cockpit-scap.spec

# Cockpit upstream shared files (fetched by make)
pkg/
test/common/
tools/

# Node
node_modules/

# Playwright — credentials and generated artifacts stay local
tests/.env
tests/screenshots/
tests/report/
tests/test-results/
test-results/

# Editor and OS files
.vscode/
.idea/
*.swp
*~
.DS_Store

# Downloaded SCAP content (SSG data streams for local testing) — large, fetched not authored
content/

# Third-party reference PDFs — copyrighted vendor docs, kept locally only
Red_Hat_Enterprise_Linux-*.pdf

# scap-tui design doc — separate project sharing this problem domain; repo-location
# question (own repo vs subdirectory here) is still open, so it isn't committed yet
SCAP_TUI_DESIGN.md
