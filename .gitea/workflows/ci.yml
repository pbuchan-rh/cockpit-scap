name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-and-lint:
    runs-on: linux-host
    steps:
      - uses: actions/checkout@v4
      - run: make pkg/lib
      - run: npm ci
      - run: npm run build
      - run: npm run eslint
      - run: npm run stylelint
